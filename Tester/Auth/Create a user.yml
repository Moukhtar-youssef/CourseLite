info:
  name: Create a user
  type: http
  seq: 1

http:
  method: POST
  url: 127.0.0.1:3000/api/auth/register
  body:
    type: json
    data: |-
      {
        "name":"try",
        "email":"test@1test.com",
        "password":"wrong123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
